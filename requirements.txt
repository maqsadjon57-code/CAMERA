# ============================================================
#  AI HUD Web Application — requirements.txt
#  Установка:  pip install -r requirements.txt
#  Python 3.10+
# ============================================================

flask>=3.0
opencv-python>=4.8,<5     # ветка 4.x: содержит HOG-детектор людей (реальный fallback);
                          # на сервере без монитора: opencv-python-headless>=4.8,<5
ultralytics>=8.1.0        # YOLOv8 (torch и torchvision установятся автоматически)
mediapipe>=0.10.9
numpy>=1.24
qrcode>=7.4               # QR-код адреса для телефона (необязательно, чистый Python)
cryptography>=42.0        # для --https (самоподписанный сертификат; не обязательно)
obd>=0.7.2                # РЕАЛЬНАЯ авто-диагностика через ELM327 (необязательно:
                          # без адаптера работает симулятор телеметрии)
